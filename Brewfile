# keni-agent 依赖清单 — `brew bundle` 一键装齐
# 主依赖只有 python3,Python 包(websockets)走 install.sh 里的 pip install

brew "python@3.12"

# 可选 NL provider CLI —— 用哪个装哪个,不装那个 provider 在 APP 远控屏会
# 报"命令未找到"
#   brew install --cask claude         # Claude Code (Anthropic 官方,需手动装)
#   brew install codex                 # OpenAI Codex CLI(假设以后上 brew)
#   brew install cursor-agent          # Cursor Agent
#   brew install openclaw              # OpenClaw
